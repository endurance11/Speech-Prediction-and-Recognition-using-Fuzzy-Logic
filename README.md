# Speech-Prediction-and-Recognition-using-Fuzzy-Logic
The following is a python program to predict the gender of a person using fuzzy logic. It uses a general dataset taken from kaggle. From the dataset the program generates a heatmap to know the correlation between the dataset elements. It also has a GUI for easy user interaction
